from typing import List, Dict


def summarize_scores(records: List[Dict]) -> Dict[str, Dict]:
    """Summarize student scores by subject.

    Args:
        records: A list of dicts, each with 'name', 'subject', and 'score' keys.

    Returns:
        A dict mapping each subject to {'average': float, 'top_scorer': str}.
    """
    pass  # Your implementation here

